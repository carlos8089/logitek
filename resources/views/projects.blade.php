@extends('layouts.backoff')
@section('boContent')
<div class="card">
    <!--
    <div class="card-header">
        <nav class="navbar">
            <div class="container-fluid">
                <span class="navbar-brand">
                    <h3><strong>{{ __('Your Projects') }}</strong></h3>
                </span>
                <form action="" class="d-flex">
                    <button class="btn btn-outline-success me-2" type="submit">Add new</button>
                </form>
            </div>
        </nav>
        
    </div>

    <div class="card-body">
        @if (session('status'))
            <div class="alert alert-success" role="alert">
                {{ session('status') }}
            </div>
        @endif
        -->
        <div style="margin: 10%; padding-left: 15%">
            <h1><strong>{{ __('Coming soon... stay focused')}}</strong></h1>
        </div>
        
    </div>
</div>
@endsection